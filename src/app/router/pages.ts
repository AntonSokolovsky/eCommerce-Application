const Pages: {
  FIRSTPAGE: string,
  CATALOG: string,
  LOGIN: string,
  REGISTER: string,
  BASKET: string,
  ACCOUNT: string,
  NOT_FOUND: string,
} = {
  FIRSTPAGE: 'main',
  CATALOG: 'catalog',
  LOGIN: 'login',
  REGISTER: 'registration',
  BASKET: 'basket',
  ACCOUNT: 'account',
  NOT_FOUND: 'not-found',
};

const ID_SELECTOR = '{id}';

export { Pages, ID_SELECTOR };